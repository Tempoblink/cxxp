/*
 * 使用与第一题中相同的三个调用，如果g的函数参数声明为T（而不是T&&），
 * 确定T的类型。如果g的函数参数是const T&呢？
 */

/*
 * 为 T
 * (a) T -> int 
 * (b) T -> const int
 * (c) T -> int
 *
 * 为 const T
 * (a) T -> int
 * (b) T -> int
 * (c) T -> int
 */

int main(int argc, char const *argv[]) {
    return 0;
}
