/*
 * 假定numbered定义了一个拷贝构造函数，能生成一个新的序号。
 * 这会改变上一题中调用的输出结果吗？如果会改变，为什么？新的输出结果是什么？
 */

/*
 * 会改变，因为mysn成员的内容是新的唯一的。
 * 新的输出结果将是3个不同的mysn，并且与a, b, c的mysn也不同。
 */

int main(int argc, char const *argv[]) {
    return 0;
}
