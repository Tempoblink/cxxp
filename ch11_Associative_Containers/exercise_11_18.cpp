/*
 * 写出第382页循环中map_it的类型，不要使用auto或decltype。
 */

/*
 * map<string,size_t> word_count;
 *
 * auto map_it = word_count.cbegin();
 *
 * map_it ->  map<string,size_t>::const_iterator;
 */

int main(int argc, char const *argv[]) {
    return 0;
}
