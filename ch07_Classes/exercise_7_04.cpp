/* 编写一个名为Person的类，使其表示人员的姓名和住址。
 * 使用string对象存放这些元素，接下来的练习将不断充实这个类的其他特征。
 */

#include <iostream>
#include <string>

struct Person {
    std::string address;
    std::string name;
};


int main(void) {
    return 0;
}
