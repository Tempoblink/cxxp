/*
 * 仅根据算法和参数的名字，描述下面每个标准库算法执行什么操作：
 *
 * replace(beg, end, old_val, new_val);
 * replace_if(beg, end, pred, new_val); 
 * replace_copy(beg, end, dest, old_val, new_val);
 * replace_copy_if(beg, end, dest, pred, new_val);
 */

/*
 * beg->end   old_val replace to new_val;
 * beg->end   if(pred) replace to new_val;
 * beg->end   old_val replace to new_val and copy to dest, orgin not change;
 * beg->end   if(pred) replace to new_val; copt to dest, orgin not change;
 */

int main(int argc, char const *argv[]) {
    return 0;
}
